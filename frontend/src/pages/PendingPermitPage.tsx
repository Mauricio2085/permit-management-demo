import { PendingPermit } from "@/components/PendingPermit";

const PendingPermitPage = () => {
  return (
    <div className="md:w-full md:flex md:flex-col md:items-center">
      <PendingPermit />
    </div>
  );
};

export { PendingPermitPage };
